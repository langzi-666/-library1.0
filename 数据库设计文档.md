# 图书管理系统数据库设计文档

## 1. 数据库概述

### 1.1 数据库信息
- **数据库名称**：library_db
- **字符集**：utf8mb4
- **排序规则**：utf8mb4_unicode_ci
- **数据库引擎**：InnoDB

### 1.2 设计原则
- 遵循第三范式，减少数据冗余
- 合理使用索引，提高查询性能
- 使用外键约束，保证数据完整性
- 字段命名规范，统一使用下划线命名法
- 为每个表添加必要的注释

## 2. 数据库表结构

### 2.1 图书分类表（category）

#### 2.1.1 表说明
用于存储图书分类信息，支持三级分类结构。

#### 2.1.2 表结构

| 字段名 | 类型 | 长度 | 是否必填 | 是否唯一 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| category_id | INT | - | 是 | 是 | AUTO_INCREMENT | 分类ID（主键） |
| category_name | VARCHAR | 50 | 是 | 否 | - | 分类名称 |
| parent_id | INT | - | 否 | 否 | 0 | 父分类ID，0表示一级分类 |
| level | INT | - | 是 | 否 | 1 | 分类级别：1/2/3 |
| description | VARCHAR | 200 | 否 | 否 | NULL | 分类描述 |
| sort_order | INT | - | 是 | 否 | 0 | 排序顺序 |
| create_time | DATETIME | - | 是 | 否 | CURRENT_TIMESTAMP | 创建时间 |

#### 2.1.3 索引设计
- PRIMARY KEY (category_id)
- INDEX (parent_id)

#### 2.1.4 数据示例
```
一级分类：文学（parent_id=0, level=1）
  └─ 二级分类：小说（parent_id=1, level=2）
      └─ 三级分类：现代小说（parent_id=2, level=3）
```

---

### 2.2 用户表（user）

#### 2.2.1 表说明
存储系统用户信息，包括管理员、图书管理员和普通用户。

#### 2.2.2 表结构

| 字段名 | 类型 | 长度 | 是否必填 | 是否唯一 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| user_id | VARCHAR | 20 | 是 | 是 | - | 用户ID（主键），格式：U+年月日+4位流水号 |
| username | VARCHAR | 50 | 是 | 是 | - | 用户名 |
| password | VARCHAR | 100 | 是 | 否 | - | 密码（加密存储） |
| name | VARCHAR | 50 | 是 | 否 | - | 姓名 |
| gender | VARCHAR | 10 | 否 | 否 | '保密' | 性别：男/女/保密 |
| phone | VARCHAR | 20 | 是 | 是 | - | 联系电话 |
| email | VARCHAR | 100 | 是 | 是 | - | 邮箱 |
| id_card | VARCHAR | 18 | 否 | 是 | NULL | 身份证号 |
| address | VARCHAR | 200 | 否 | 否 | NULL | 地址 |
| role | VARCHAR | 20 | 是 | 否 | '普通用户' | 角色：系统管理员/图书管理员/普通用户 |
| status | VARCHAR | 20 | 是 | 否 | '正常' | 状态：正常/锁定/已注销 |
| register_date | DATETIME | - | 是 | 否 | CURRENT_TIMESTAMP | 注册日期 |
| last_login_time | DATETIME | - | 否 | 否 | NULL | 最后登录时间 |
| create_time | DATETIME | - | 是 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | - | 是 | 否 | CURRENT_TIMESTAMP | 更新时间 |

#### 2.2.3 索引设计
- PRIMARY KEY (user_id)
- UNIQUE KEY (username)
- UNIQUE KEY (email)
- UNIQUE KEY (phone)
- INDEX (role)
- INDEX (status)

#### 2.2.4 业务规则
- user_id格式：U + YYYYMMDD + 4位流水号（如：U20241201001）
- 密码使用MD5或SHA256加密存储
- 用户名、邮箱、手机号必须唯一
- 默认角色为"普通用户"

---

### 2.3 图书表（book）

#### 2.3.1 表说明
存储图书的基本信息，包括图书编号、书名、作者、出版社、ISBN等。

#### 2.3.2 表结构

| 字段名 | 类型 | 长度 | 是否必填 | 是否唯一 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| book_id | VARCHAR | 20 | 是 | 是 | - | 图书编号（主键），格式：BK+年月日+4位流水号 |
| title | VARCHAR | 200 | 是 | 否 | - | 书名 |
| author | VARCHAR | 100 | 是 | 否 | - | 作者，多个作者用逗号分隔 |
| publisher | VARCHAR | 100 | 是 | 否 | - | 出版社 |
| publish_date | DATE | - | 是 | 否 | - | 出版日期 |
| isbn | VARCHAR | 20 | 是 | 是 | - | ISBN号 |
| category_id | INT | - | 是 | 否 | - | 分类ID（外键） |
| price | DECIMAL | 10,2 | 是 | 否 | 0.00 | 价格 |
| stock | INT | - | 是 | 否 | 0 | 库存数量 |
| available_stock | INT | - | 是 | 否 | 0 | 可用库存（库存-借出数量） |
| description | TEXT | - | 否 | 否 | NULL | 图书描述 |
| cover_image | VARCHAR | 200 | 否 | 否 | NULL | 封面图片路径 |
| status | VARCHAR | 20 | 是 | 否 | '在库' | 状态：在库/已借出/已下架/损坏 |
| create_time | DATETIME | - | 是 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | - | 是 | 否 | CURRENT_TIMESTAMP | 更新时间 |

#### 2.3.3 索引设计
- PRIMARY KEY (book_id)
- UNIQUE KEY (isbn)
- INDEX (title)
- INDEX (author)
- INDEX (category_id)
- INDEX (status)
- FOREIGN KEY (category_id) REFERENCES category(category_id)

#### 2.3.4 业务规则
- book_id格式：BK + YYYYMMDD + 4位流水号（如：BK20241201001）
- ISBN必须唯一
- stock >= 0（库存不能为负数）
- available_stock = stock - 当前借出数量
- 状态说明：
  - 在库：图书可借阅
  - 已借出：所有图书都被借出
  - 已下架：图书不再对外借阅
  - 损坏：图书已损坏

---

### 2.4 借阅记录表（borrow_record）

#### 2.4.1 表说明
记录用户借阅图书的详细信息，包括借阅日期、应还日期、归还日期等。

#### 2.4.2 表结构

| 字段名 | 类型 | 长度 | 是否必填 | 是否唯一 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| record_id | INT | - | 是 | 是 | AUTO_INCREMENT | 记录ID（主键） |
| user_id | VARCHAR | 20 | 是 | 否 | - | 用户ID（外键） |
| book_id | VARCHAR | 20 | 是 | 否 | - | 图书编号（外键） |
| borrow_date | DATETIME | - | 是 | 否 | CURRENT_TIMESTAMP | 借阅日期 |
| due_date | DATETIME | - | 是 | 否 | - | 应还日期 |
| return_date | DATETIME | - | 否 | 否 | NULL | 归还日期 |
| renew_count | INT | - | 是 | 否 | 0 | 续借次数 |
| status | VARCHAR | 20 | 是 | 否 | '借阅中' | 状态：借阅中/已归还/已逾期 |
| overdue_days | INT | - | 否 | 否 | 0 | 逾期天数 |
| overdue_fee | DECIMAL | 10,2 | 否 | 否 | 0.00 | 逾期费用 |
| book_status | VARCHAR | 20 | 否 | 否 | '正常' | 归还时图书状态：正常/损坏/丢失 |
| remark | VARCHAR | 500 | 否 | 否 | NULL | 备注 |
| create_time | DATETIME | - | 是 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | - | 是 | 否 | CURRENT_TIMESTAMP | 更新时间 |

#### 2.4.3 索引设计
- PRIMARY KEY (record_id)
- INDEX (user_id)
- INDEX (book_id)
- INDEX (status)
- INDEX (borrow_date)
- INDEX (due_date)
- FOREIGN KEY (user_id) REFERENCES user(user_id)
- FOREIGN KEY (book_id) REFERENCES book(book_id)

#### 2.4.4 业务规则
- 借阅时：status='借阅中'，return_date=NULL
- 归还时：status='已归还'，return_date=归还日期
- 逾期计算：如果当前日期 > due_date 且 status='借阅中'，则标记为逾期
- 逾期天数 = 当前日期 - due_date
- 逾期费用 = 逾期天数 × 逾期费率（从system_config表获取）

---

### 2.5 操作日志表（operation_log）

#### 2.5.1 表说明
记录用户的重要操作，便于追溯和审计。

#### 2.5.2 表结构

| 字段名 | 类型 | 长度 | 是否必填 | 是否唯一 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| log_id | INT | - | 是 | 是 | AUTO_INCREMENT | 日志ID（主键） |
| user_id | VARCHAR | 20 | 是 | 否 | - | 操作用户ID |
| operation_type | VARCHAR | 50 | 是 | 否 | - | 操作类型：登录/登出/借阅/归还/添加图书等 |
| operation_content | TEXT | - | 否 | 否 | NULL | 操作内容 |
| ip_address | VARCHAR | 50 | 否 | 否 | NULL | IP地址 |
| user_agent | VARCHAR | 200 | 否 | 否 | NULL | 用户代理 |
| operation_time | DATETIME | - | 是 | 否 | CURRENT_TIMESTAMP | 操作时间 |
| result | VARCHAR | 20 | 是 | 否 | '成功' | 操作结果：成功/失败 |

#### 2.5.3 索引设计
- PRIMARY KEY (log_id)
- INDEX (user_id)
- INDEX (operation_type)
- INDEX (operation_time)

#### 2.5.4 业务规则
- 记录所有敏感操作：登录、登出、借阅、归还、添加/删除图书、修改用户信息等
- operation_type枚举值：
  - 登录、登出
  - 借阅、归还、续借
  - 添加图书、编辑图书、删除图书
  - 添加用户、编辑用户、删除用户
  - 修改密码、修改个人信息

---

### 2.6 系统设置表（system_config）

#### 2.6.1 表说明
存储系统配置参数，如借阅规则、系统参数等。

#### 2.6.2 表结构

| 字段名 | 类型 | 长度 | 是否必填 | 是否唯一 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| config_id | INT | - | 是 | 是 | AUTO_INCREMENT | 配置ID（主键） |
| config_key | VARCHAR | 50 | 是 | 是 | - | 配置键 |
| config_value | TEXT | - | 是 | 否 | - | 配置值 |
| config_desc | VARCHAR | 200 | 否 | 否 | NULL | 配置描述 |
| update_time | DATETIME | - | 是 | 否 | CURRENT_TIMESTAMP | 更新时间 |

#### 2.6.3 索引设计
- PRIMARY KEY (config_id)
- UNIQUE KEY (config_key)

#### 2.6.4 默认配置项

| config_key | config_value | config_desc |
|------------|--------------|-------------|
| borrow_days | 30 | 借阅期限（天） |
| max_borrow_count | 5 | 普通用户最大借阅数量 |
| renew_count | 1 | 续借次数 |
| renew_days | 30 | 续借期限（天） |
| overdue_fee_per_day | 1.00 | 逾期费用（元/天） |
| stock_warning_threshold | 5 | 库存预警阈值 |
| system_name | 图书管理系统 | 系统名称 |
| system_version | 1.0.0 | 系统版本 |

---

## 3. 表关系图

```
┌─────────────┐
│   category  │
│  (分类表)    │
└──────┬──────┘
       │
       │ 1:N
       │
┌──────▼──────┐
│    book     │
│  (图书表)    │
└──────┬──────┘
       │
       │ 1:N
       │
┌──────▼─────────────┐      ┌─────────────┐
│  borrow_record     │──────│    user      │
│  (借阅记录表)       │      │  (用户表)     │
└────────────────────┘      └─────────────┘
       │
       │
┌──────▼─────────────┐
│  operation_log     │
│  (操作日志表)       │
└────────────────────┘

┌────────────────────┐
│  system_config      │
│  (系统设置表)        │
└────────────────────┘
```

## 4. 数据库设计说明

### 4.1 命名规范
- 表名：使用小写字母和下划线，复数形式（如：borrow_records）
- 字段名：使用小写字母和下划线（如：user_id）
- 索引名：idx_字段名（如：idx_user_id）
- 外键名：fk_表名_关联表名（如：fk_book_category）

### 4.2 数据类型选择
- 整型：INT（用于ID、计数等）
- 字符串：VARCHAR（固定长度）或TEXT（可变长度）
- 日期时间：DATETIME（精确到秒）
- 金额：DECIMAL(10,2)（精确到分）
- 布尔值：使用VARCHAR或TINYINT

### 4.3 索引设计原则
- 主键自动创建聚簇索引
- 外键字段创建索引
- 经常用于查询条件的字段创建索引
- 经常用于排序的字段创建索引
- 避免过度索引，影响写入性能

### 4.4 外键约束
- 使用外键约束保证数据完整性
- 删除操作需要考虑级联关系
- 外键字段必须有对应的索引

### 4.5 数据完整性
- 使用NOT NULL约束保证必填字段
- 使用UNIQUE约束保证唯一性
- 使用CHECK约束（MySQL 8.0+）或应用层校验保证数据有效性
- 使用外键约束保证参照完整性

## 5. 性能优化建议

### 5.1 查询优化
- 合理使用索引，避免全表扫描
- 避免SELECT *，只查询需要的字段
- 使用分页查询，避免一次查询大量数据
- 复杂查询考虑使用视图或存储过程

### 5.2 索引优化
- 定期分析查询语句，优化索引
- 使用EXPLAIN分析查询计划
- 避免在索引字段上使用函数
- 联合索引遵循最左前缀原则

### 5.3 数据归档
- 定期归档历史日志数据
- 对于大量历史数据，考虑分表或归档
- 借阅记录表数据量大时，考虑按时间分表

## 6. 备份和恢复

### 6.1 备份策略
- 每日自动备份数据库
- 备份文件保留30天
- 重要操作前手动备份

### 6.2 恢复策略
- 定期测试备份文件的恢复能力
- 记录恢复操作步骤
- 准备灾难恢复预案

---

**文档版本**：v1.0  
**创建日期**：2024  
**最后更新**：2024  
**文档作者**：项目组


