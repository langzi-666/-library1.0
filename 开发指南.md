# 开发指南

## 第一周任务完成情况

### ✅ 已完成任务

1. **项目框架搭建**
   - ✅ 后端Spring Boot项目框架
   - ✅ 前端Vue 3项目框架
   - ✅ 项目基础配置

2. **数据库设计**
   - ✅ 数据库表结构设计（6张表）
   - ✅ 数据库初始化SQL脚本
   - ✅ 数据库设计文档

3. **基础配置**
   - ✅ 后端配置文件（开发/生产环境）
   - ✅ 前端项目配置
   - ✅ 跨域配置
   - ✅ 统一响应结果类

## 快速启动

### 前置条件

1. 安装JDK 11+
2. 安装Maven 3.6+
3. 安装Node.js 16+
4. 安装MySQL 8.0+

### 步骤一：初始化数据库

```bash
# 登录MySQL
mysql -u root -p

# 执行初始化脚本
source database/init.sql
```

或者使用MySQL客户端工具（如Navicat、DBeaver）执行 `database/init.sql` 文件。

### 步骤二：配置数据库连接

编辑 `backend/src/main/resources/application.yml`：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/library_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: your_password  # 修改为你的MySQL密码
```

### 步骤三：启动后端服务

```bash
cd backend

# 方式1：使用Maven命令
mvn spring-boot:run

# 方式2：使用IDE运行
# 直接运行 LibraryManagementSystemApplication.java
```

后端服务启动后：
- 服务地址：http://localhost:8080/api
- API文档：http://localhost:8080/api/doc.html

### 步骤四：启动前端服务

```bash
cd frontend

# 安装依赖（首次运行）
npm install

# 启动开发服务器
npm run dev
```

前端服务启动后：
- 访问地址：http://localhost:3000

## 项目结构说明

### 后端结构

```
backend/
├── src/main/java/com/library/
│   ├── common/              # 通用类
│   │   ├── Result.java      # 统一响应结果
│   │   └── BaseEntity.java  # 实体基类
│   ├── config/              # 配置类
│   │   └── WebConfig.java   # Web配置（跨域等）
│   ├── controller/          # 控制器（待开发）
│   ├── service/             # 服务层（待开发）
│   ├── mapper/              # Mapper接口（待开发）
│   ├── entity/              # 实体类（待开发）
│   └── LibraryManagementSystemApplication.java
└── src/main/resources/
    ├── application.yml      # 主配置文件
    ├── application-dev.yml  # 开发环境配置
    └── application-prod.yml # 生产环境配置
```

### 前端结构

```
frontend/
├── src/
│   ├── components/          # 公共组件（待开发）
│   ├── views/               # 页面组件
│   │   ├── Login.vue        # 登录页
│   │   └── Dashboard.vue    # 首页
│   ├── router/              # 路由配置
│   │   └── index.js
│   ├── utils/               # 工具函数
│   │   └── request.js       # Axios封装
│   ├── stores/              # Pinia状态管理（待开发）
│   ├── App.vue              # 根组件
│   └── main.js              # 入口文件
└── package.json
```

### 数据库结构

```
library_db/
├── category         # 图书分类表
├── user             # 用户表
├── book             # 图书表
├── borrow_record    # 借阅记录表
├── operation_log    # 操作日志表
└── system_config    # 系统设置表
```

## 下一步开发计划

### 第二周：用户管理模块

需要开发的功能：
1. 用户注册功能
   - 创建User实体类
   - 创建UserMapper接口
   - 创建UserService服务
   - 创建UserController控制器
   - 实现注册接口

2. 用户登录功能
   - JWT工具类
   - 登录接口
   - 验证码功能
   - 登录拦截器

3. 用户信息管理
   - 用户信息查询
   - 用户信息修改
   - 用户删除

4. 权限控制
   - 权限拦截器
   - 角色验证
   - 权限注解

5. 操作日志
   - 日志记录功能
   - 日志查询接口

## 开发规范

### 后端开发规范

1. **包命名规范**
   - `controller` - 控制器层
   - `service` - 服务层
   - `mapper` - 数据访问层
   - `entity` - 实体类
   - `dto` - 数据传输对象
   - `vo` - 视图对象
   - `common` - 通用类

2. **代码规范**
   - 使用Lombok简化代码
   - 统一使用Result作为返回类型
   - 实体类继承BaseEntity
   - 使用MyBatis Plus简化CRUD操作

3. **API设计规范**
   - 遵循RESTful风格
   - 使用统一的URL前缀 `/api`
   - 使用统一的响应格式

### 前端开发规范

1. **组件命名**
   - 组件名使用PascalCase
   - 文件名与组件名保持一致

2. **代码规范**
   - 使用Composition API
   - 使用Pinia管理状态
   - 使用Element Plus组件库
   - 统一使用request.js发送请求

3. **路由规范**
   - 路由路径使用kebab-case
   - 路由名称使用PascalCase
   - 使用路由守卫进行权限验证

## 常见问题

### 1. 数据库连接失败

**问题**：启动后端服务时提示数据库连接失败

**解决方案**：
- 检查MySQL服务是否启动
- 检查数据库连接配置是否正确
- 检查数据库是否已创建（library_db）
- 检查用户名密码是否正确

### 2. 前端无法连接后端

**问题**：前端请求后端接口时提示跨域错误

**解决方案**：
- 检查后端是否启动
- 检查后端端口是否为8080
- 检查前端代理配置（vite.config.js）

### 3. 依赖安装失败

**问题**：npm install 或 mvn install 失败

**解决方案**：
- 检查网络连接
- 尝试使用国内镜像源
- 清除缓存后重试

## 参考资料

- [Spring Boot官方文档](https://spring.io/projects/spring-boot)
- [Vue 3官方文档](https://cn.vuejs.org/)
- [Element Plus文档](https://element-plus.org/zh-CN/)
- [MyBatis Plus文档](https://baomidou.com/)

---

**文档版本**：v1.0  
**更新日期**：2024


